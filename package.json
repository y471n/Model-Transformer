{
  "name": "model-transformer",
  "version": "1.0.0",
  "scripts": {
    "test": "yarn --cwd ./packages/test test",
    "build": "yarn --cwd ./packages/model-transformer build",
    "deploy": "yarn --cwd ./package/model-transformer publish",
    "updateVersion": "node helper.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "repository": "https://github.com/RastogiAbhijeet/Model-Transformer",
  "author": "",
  "license": "MIT"
}
